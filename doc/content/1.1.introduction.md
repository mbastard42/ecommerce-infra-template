<div align="center">

[< *Summary*](0.0.index.md) | [**Summary**](0.0.index.md) | [*Architecture* >](1.2.architecture.md)

## Documentation

### I.1. Platform - Introduction

</div>

<div align="center">

[the](1.2.architecture.md#) | [sub](1.2.architecture.md#) | [menu](1.2.architecture.md#)
</div>

Parfait, on reste au niveau concepts / briques à mettre en place, sans rentrer dans le détail technique pour l’instant 👇

⸻

1. Structure globale des environnements
	1.	Séparation claire des env
	•	dev, staging, prod
	•	mais mêmes services, mêmes ports internes, mêmes variables (juste valeurs différentes)
	2.	Fichiers de config par environnement
	•	ex : .env, .env.development, .env.production, .env.local
	•	docker-compose.yml + docker-compose.dev.yml (override dev)
	3.	Nommage et conventions
	•	noms de services stables : frontend, api, db, odoo, seaweed, caddy, etc.
	•	mêmes hostnames internes en dev / prod (api, odoo, storage…)

⸻

2. Gestion propre des variables d’environnement
	4.	Source de vérité unique
	•	un .env.example documenté
	•	même clés d’ENV pour dev et prod (APP_ENV, APP_URL, DATABASE_URL, STORAGE_ENDPOINT, etc.)
	5.	Séparation config / secrets
	•	config versionnée (URLs, features flags)
	•	secrets non versionnés (mots de passe, clés API)
	6.	Chargement homogène
	•	tout passe par ENV (pas de config “magique” hardcodée dans le code)
	•	côté SvelteKit : env publics vs privés (PUBLIC_…)

⸻

3. Docker : images, builds et perf en dev
	7.	Images de base communes dev/prod
	•	mêmes Dockerfile de base
	•	dev = prod + quelques options (watchers, debug) via docker-compose.dev.yml / entrypoint
	8.	Multi-stage builds optimisés
	•	ordre des layers → dépendances d’abord, code ensuite
	•	pour le front : node_modules mise en cache
	•	pour Symfony : vendor + cache de composer/apt
	9.	Montage du code en volume en dev
	•	.:/app ou équivalent → hot reload sans rebuild d’image complet
	10.	Rebuild ciblés

	•	un service = un conteneur = un Dockerfile logique
	•	pouvoir rebuild un service sans redémarrer tout l’écosystème

⸻

4. Hot reload & DX (Developer Experience)
	11.	Hot reload front

	•	SvelteKit en mode dev (Vite) dans un container ou sur la machine, mais toujours connecté aux mêmes URLs backend que la prod (reverse proxy Caddy, etc.)

	12.	Hot reload back

	•	Symfony : watcher (symfony server / PHP-FPM + watcher, ou outil type entr, reflex…)
	•	Odoo dev : autoreload / restart léger au changement de modules custom

	13.	Hot reload docs / tools éventuels

	•	si tu as un back interne (API Rust, Node, etc.), même logique : watcher de fichiers et reload automatique

⸻

5. Debug & introspection
	14.	Debug PHP / Symfony

	•	Xdebug ou équivalent pour le remote debug depuis VS Code
	•	mapping des paths host/container pour que les breakpoints marchent

	15.	Debug front

	•	DevTools / Svelte Devtools
	•	sourcemaps activées en dev

	16.	Debug DB & services

	•	accès simple à pgadmin, adminer, grafana, etc. sur des ports dédiés
	•	comptes dev spécifiques

⸻

6. Lifecycle de la base de données & des données
	17.	Bases séparées par env

	•	app_dev, app_test, app_prod (ou schémas dédiés)

	18.	Scripts standardisés

	•	migrate, reset, seed automatisés (via Makefile/script)
	•	possibilité de repart de zéro facilement en dev

	19.	Données de dev réalistes

	•	fixtures cohérentes (produits, utilisateurs, commandes)
	•	éventuellement anonymisation de dumps de prod réimportables en dev

⸻

7. Stockage de fichiers / objets (SeaweedFS, MinIO, etc.)
	20.	Endoints “comme en prod”

	•	mêmes noms de buckets / collections
	•	même manière de générer les URLs que ce que Caddy servira

	21.	Volumes persistants

	•	volumes Docker nommés pour le storage en dev

	22.	Data reset / clean

	•	scripts pour vider / reseed le storage si besoin

⸻

8. Réseau interne & reverse proxy
	23.	Réseau Docker cohérent

	•	un ou plusieurs networks (frontend, backend, data) mais nommés pareil dev/prod

	24.	Reverse proxy unique (Caddy/nginx)

	•	même routing logique qu’en prod (mêmes chemins pour /api, /storage, /grafana, etc.)
	•	en dev, SSL simplifié (HTTP ou cert auto)

	25.	Résolution des noms

	•	hostnames simples : http://api, http://odoo, http://storage, etc. plutôt que des IPs

⸻

9. Observabilité en dev
	26.	Logs centralisés

	•	logs de chaque service accessibles facilement (docker compose logs, ou UI style Loki/Grafana plus tard)
	•	format de logs proche prod (json loggable)

	27.	Healthchecks homogènes

	•	tu as déjà ton /health global → chaque service doit exposer un healthcheck basique (HTTP/DB)

	28.	(Optionnel) Metrics & traces

	•	base Prometheus/Grafana si tu veux pousser le truc
	•	même labels et métriques qu’en prod, mais petit volume

⸻

10. Tests & qualité
	29.	Pyramide de tests

	•	unitaires (front/back)
	•	intégration (API + DB)
	•	éventuellement E2E (Playwright/Cypress) contre la stack docker dev

	30.	Mode test isolé

	•	docker-compose.test.yml minimal (API + DB + dépendances strictes)

	31.	Qualité de code

	•	linter / formatter (ESLint, Prettier, PHP-CS-Fixer, etc.)
	•	scripts lint, test, check réutilisés par le CI

⸻

11. Outils de pilotage (Makefile / Task runner)
	32.	Commandes one-liner

	•	make dev → lance la stack dev
	•	make stop, make logs, make shell-api, make shell-frontend
	•	make reset-db, make seed, etc.

	33.	Ciblage par service

	•	make api-rebuild, make frontend-restart, etc.

	34.	Documenter ces commandes

	•	README / docs pour toi + futurs devs

⸻

12. Cohérence avec la prod / NixOS & CI/CD
	35.	Même artefact que en prod

	•	les images utilisées en dev sont les mêmes que celles construites par le CI (ou très proches)
	•	seulement l’entrypoint / config change

	36.	Alignement avec NixOS & déploiements

	•	ce que Nix déploie en prod doit ressembler à ce que Docker compose lance en dev (mêmes services, mêmes options clé)

	37.	Pas de “magie locale”

	•	éviter les trucs qui ne marchent que sur ta machine (paths custom, tools pas documentés)

⸻

13. Documentation & onboard
	38.	Doc “setup dev”

	•	prérequis (Docker, Make, éventuel direnv/nix)
	•	pas-à-pas pour démarrer la stack

	39.	Doc “workflow”

	•	comment lancer le front, l’API, Odoo, etc.
	•	comment ajouter un nouveau service dans la stack dev (pattern réutilisable)

	40.	Doc “troubleshooting”

	•	ports qui clash, pb de droits volumes, migrations qui plantent, etc.

⸻

Si tu veux, ensuite on peut :
	•	prendre ta stack actuelle (SvelteKit + Symfony + Odoo + Seaweed + Grafana/pgAdmin/Caddy…),
	•	et descendre point par point en mode : “OK, pour le point 7, on fait tel Dockerfile et tel docker-compose.dev.yml”, avec les vrais fichiers adaptés à ton projet.

</div>

<div align="center">

#
[< *Summary*](0.0.index.md) | [**Summary**](0.0.index.md) | [*Architecture* >](1.2.architecture.md)
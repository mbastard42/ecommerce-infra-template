localhost {
    tls internal
    encode zstd gzip

    handle /health {
        reverse_proxy backend:8080
    }

    handle {
        root * /usr/share/caddy
        try_files {path} /index.html
        file_server
    }

    log {
        output stdout
    }
}
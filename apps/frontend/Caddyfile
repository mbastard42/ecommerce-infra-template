localhost {
	
	tls internal
	encode zstd gzip

	root * /usr/share/caddy
	try_files {path} /index.html

	file_server

	    handle /health {
        reverse_proxy backend:8000
    }

	log {
		output stdout
	}
}